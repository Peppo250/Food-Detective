# Examples Directory

This folder contains example food label images that can be used to test the Food Detective scanner.

## How to test
1. Run the application (`python main.py` or `python scripts/run.py`).
2. Click **Pick from Files**.
3. Choose one of the sample images stored in this directory to see the ingredients parsed and scored.
